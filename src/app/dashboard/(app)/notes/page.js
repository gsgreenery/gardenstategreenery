import { NotesView } from "../../DashboardViews";

export const metadata = {
  title: "Crew Notes | Garden State Greenery",
};

export default function DashboardNotesPage() {
  return <NotesView />;
}
